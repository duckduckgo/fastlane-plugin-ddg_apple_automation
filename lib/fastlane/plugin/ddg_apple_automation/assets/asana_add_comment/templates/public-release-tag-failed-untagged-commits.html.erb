<body>
  <h2>[ACTION NEEDED] <%= tag %> public release not tagged – unreleased commits found</h2>
  <ul>
    <li><b>❗️ Tagging the repository with <%= tag %> public release tag was aborted because untagged commits were found on the <%= branch %> branch.</b>
    <li>Top commit is <a href='<%= untagged_commit_url %>'><code><%= untagged_commit_sha %></code></a>.</li>
    <li><b>⚠️ GitHub release wasn't created.</b></li>
    <li><b>⚠️ <code><%= branch %></code> branch wasn't deleted.</b></li>
  </ul>
  <br>
  <a data-asana-gid='<%= assignee_id %>' />, please follow instructions to remove untagged commits proceed with the release <a data-asana-gid='<%= task_id %>'
    data-asana-dynamic='false'>according to instructions</a>.<br>
  <br>
  🔗 Workflow URL: <a href='<%= workflow_url %>'><%= workflow_url %></a>.
</body>